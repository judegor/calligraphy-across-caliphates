baseURL = 'https://aibaniz.github.io/calligraphy-across-caliphates/'
languageCode = 'en-us'
title = 'Calligraphy Across the Caliphates'
theme = ''  # Empty if no theme is used
enableRobotsTXT = true

[params]
  custom_css = ["custom.css"]

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true  # Allows inline HTML in Markdown

[outputs]
  home = ["HTML"]
